# PolyBot - dependencias de Python
# Instalar con: ./venv/bin/pip install -r requirements.txt

# --- Core async + HTTP ---
aiohttp>=3.9.0
httpx>=0.27.0
requests>=2.31.0

# --- Config ---
python-dotenv>=1.0.0

# --- Polymarket / Polygon ---
py-clob-client-v2>=1.0.0     # SDK v2 (post-migración 28-Abr-2026)
py-clob-client>=0.18.0       # legacy v1, mantenido para compatibilidad
web3>=6.15.0
eth-account>=0.11.0

# --- IA (opcional, solo si activas MARKET SCANNER) ---
anthropic>=0.40.0

# --- Datos de mercado / news ---
feedparser>=6.0.0            # RSS news para STOCK TRADER

# --- Dashboard web ---
flask>=3.0.0
